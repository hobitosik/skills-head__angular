import { PictureSizesDirective } from './picture-sizes.directive';

describe('PictureSizesDirective', () => {
  it('should create an instance', () => {
    const directive = new PictureSizesDirective();
    expect(directive).toBeTruthy();
  });
});
